#include <stdio.h>
#include <stdlib.h>

/**
 * main - contains the functional block of code
 * @argc: contains number of arguments
 * @argv: contains arrays of value of arguments
 * Return: Always 0.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s \n", argv[0]);
	return (0);
}
